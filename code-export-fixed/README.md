# المتجر الإلكتروني - المشروع الكامل

## 📦 محتويات المشروع

```
exported-project/
├── flutter/           # تطبيق Flutter للموبايل (بدون تغيير)
├── php/              # واجهة برمجة التطبيقات (API) - معدّل للمتجر
├── sql/              # قاعدة البيانات - معدّل للمتجر
└── web/              # موقع الويب - معدّل للمتجر
```

## ✅ التعديلات المُنفذة

### تم تحويل الويب من "منصة كورسات" إلى "متجر إلكتروني":

| قبل (كورسات) | بعد (متجر) |
|-------------|-----------|
| courses | products |
| lessons | - |
| enrollments | orders + order_items |
| instructor | - |
| دورات | منتجات |
| التسجيل في الدورة | إضافة للسلة |
| تقدم الدورة | حالة الطلب |

### الملفات المعدّلة:

1. **sql/database.sql** - قاعدة بيانات جديدة للمتجر
2. **web/index.html** - الصفحة الرئيسية للمتجر
3. **web/css/style.css** - تصميم المتجر
4. **web/js/config.js** - إعدادات المتجر
5. **web/js/api.js** - API للمنتجات والسلة والطلبات
6. **web/js/app.js** - التطبيق الرئيسي
7. **web/js/auth.js** - المصادقة
8. **php/api/products.php** - API المنتجات
9. **php/api/cart.php** - API السلة
10. **php/api/orders.php** - API الطلبات
11. **php/config/config.php** - إعدادات PHP

## 🚀 التثبيت

### 1. قاعدة البيانات
```bash
mysql -u root -p < sql/database.sql
```

### 2. PHP API
```bash
cp -r php /var/www/html/ecommerce-api
nano php/config/config.php  # عدّل بيانات الاتصال
```

### 3. موقع الويب
```bash
cp -r web /var/www/html/ecommerce
nano web/js/config.js  # عدّل API_URL
```

### 4. Flutter (بدون تغيير)
```bash
cd flutter && flutter pub get && flutter run
```

## 🔐 بيانات الاختبار

- **المدير:** admin@example.com / admin123
- **العميل:** customer@example.com / password
