# منصة الدورات التعليمية - المشروع الكامل

## 📦 محتويات المشروع

```
code-export-fixed/
├── flutter/           # تطبيق Flutter للموبايل
├── php/              # واجهة برمجة التطبيقات (API)
├── sql/              # قاعدة البيانات
└── web/              # موقع الويب
```

## 🚀 التثبيت الكامل

### 1. قاعدة البيانات (MySQL)

```bash
cd sql
mysql -u root -p < database.sql
```

### 2. واجهة API (PHP)

```bash
# انسخ مجلد php إلى مجلد الخادم
cp -r php /var/www/html/courses-platform

# عدّل إعدادات قاعدة البيانات
nano php/config/database.php

# أنشئ مجلد الرفع
mkdir -p php/uploads
chmod 755 php/uploads
```

### 3. موقع الويب

```bash
# انسخ مجلد web إلى مجلد الخادم
cp -r web /var/www/html/courses-platform/public

# عدّل عنوان API
nano web/js/config.js
```

### 4. تطبيق Flutter

```bash
cd flutter

# تثبيت المكتبات
flutter pub get

# تشغيل التطبيق
flutter run
```

## ✅ الميزات المُنفذة

### قاعدة بيانات ديناميكية
- ✅ SQLite للتطبيق (Flutter)
- ✅ MySQL للخادم (PHP)
- ✅ عمليات CRUD كاملة

### المصادقة
- ✅ تسجيل حساب جديد
- ✅ تسجيل الدخول
- ✅ تسجيل الخروج
- ✅ JWT Token

### التحقق من المدخلات (Validation)
- ✅ البريد الإلكتروني
- ✅ كلمة المرور
- ✅ الحقول المطلوبة
- ✅ الحدود القصوى والدنيا

### صلاحيات الموبايل (Permissions)
- ✅ الكاميرا
- ✅ التخزين
- ✅ الإشعارات
- ✅ الصور

### الدورات
- ✅ عرض الدورات
- ✅ تفاصيل الدورة
- ✅ البحث والتصفية
- ✅ التسجيل في الدورات
- ✅ تتبع التقدم

### سلة التسوق
- ✅ إضافة/إزالة
- ✅ عرض المجموع
- ✅ إتمام الشراء

### التقييمات
- ✅ عرض التقييمات
- ✅ إضافة تقييم
- ✅ الإحصائيات

## 📚 التوثيق

- [Flutter README](flutter/README.md)
- [PHP API README](php/README.md)
- [Web README](web/README.md)

## 🔐 بيانات الاختبار

### المدير
- البريد: admin@example.com
- كلمة المرور: password

### المدرب
- البريد: instructor@example.com
- كلمة المرور: password

### الطالب
- البريد: student@example.com
- كلمة المرور: password

## 🛠 التقنيات المستخدمة

| المكون | التقنية |
|--------|---------|
| تطبيق الموبايل | Flutter + SQLite |
| واجهة API | PHP + PDO |
| قاعدة البيانات | MySQL |
| موقع الويب | HTML + CSS + JavaScript |
| المصادقة | JWT |

## 📝 ملاحظات

1. **الأمان:** تأكد من تغيير `JWT_SECRET` في الإنتاج
2. **قاعدة البيانات:** حدّث بيانات الاتصال في `config/database.php`
3. **الرفع:** تأكد من صلاحيات مجلد `uploads`
4. **Flutter:** أضف مفاتيح Firebase إذا أردت استخدامه

## 🆘 الدعم

في حال وجود أي مشاكل، راجع ملفات README الخاصة بكل مكون.
