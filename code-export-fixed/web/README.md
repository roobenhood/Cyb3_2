# Web Frontend - منصة الدورات التعليمية

## 📁 هيكل المشروع

```
web/
├── css/
│   └── style.css          # الأنماط الرئيسية
├── js/
│   ├── config.js          # إعدادات التطبيق
│   ├── api.js             # خدمة API
│   ├── auth.js            # خدمة المصادقة والتحقق
│   └── app.js             # التطبيق الرئيسي
├── images/                # الصور
├── index.html             # الصفحة الرئيسية
├── login.html             # صفحة تسجيل الدخول
├── register.html          # صفحة إنشاء حساب
├── courses.html           # صفحة الدورات
├── course.html            # صفحة تفاصيل الدورة
├── cart.html              # صفحة السلة
├── profile.html           # صفحة الملف الشخصي
└── my-courses.html        # صفحة دوراتي
```

## 🚀 التثبيت

1. **رفع الملفات:**
   - ارفع مجلد `web` إلى الخادم

2. **تعديل الإعدادات:**
   - عدّل ملف `js/config.js` وغيّر `API_URL` إلى عنوان API الصحيح

## ✨ الميزات

### المصادقة
- تسجيل حساب جديد مع التحقق من المدخلات
- تسجيل الدخول مع JWT Token
- حماية الصفحات المحمية
- تسجيل الخروج

### الدورات
- عرض الدورات المميزة
- تصفية حسب التصنيف والمستوى
- بحث في الدورات
- ترتيب النتائج
- تفاصيل الدورة مع الدروس

### سلة التسوق
- إضافة/إزالة الدورات
- عرض المجموع
- إتمام الشراء

### التقييمات
- عرض تقييمات الدورة
- إضافة تقييم (للمسجلين)
- إحصائيات التقييم

## 🔐 التحقق من المدخلات

جميع النماذج تحتوي على تحقق كامل:
- الحقول المطلوبة
- صحة البريد الإلكتروني
- طول كلمة المرور (6 أحرف على الأقل)
- تطابق تأكيد كلمة المرور
- تنظيف المدخلات من XSS

## 📱 التصميم المتجاوب

- يعمل على جميع الشاشات
- قائمة موبايل مخصصة
- تخطيط مرن للكروت

## 🎨 التخصيص

يمكنك تعديل الألوان من خلال CSS Variables في `style.css`:

```css
:root {
    --primary-color: #4f46e5;
    --secondary-color: #10b981;
    --accent-color: #f59e0b;
    /* ... */
}
```

## 🔧 إضافة صفحات جديدة

1. أنشئ ملف HTML جديد
2. أضف نفس الـ scripts في نهاية الملف:
```html
<script src="js/config.js"></script>
<script src="js/api.js"></script>
<script src="js/auth.js"></script>
<script src="js/app.js"></script>
```
3. أضف منطق الصفحة في `app.js`
