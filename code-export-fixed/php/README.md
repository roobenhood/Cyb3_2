# PHP API Backend - منصة الدورات التعليمية

## 📁 هيكل المشروع

```
php/
├── config/
│   ├── config.php      # إعدادات التطبيق
│   └── database.php    # اتصال قاعدة البيانات
├── utils/
│   ├── Validator.php   # التحقق من المدخلات
│   ├── Response.php    # مساعد الاستجابة
│   ├── Auth.php        # المصادقة JWT
│   └── FileUpload.php  # رفع الملفات
├── models/
│   ├── User.php        # نموذج المستخدم
│   ├── Course.php      # نموذج الدورة
│   ├── Lesson.php      # نموذج الدرس
│   ├── Category.php    # نموذج التصنيف
│   ├── Enrollment.php  # نموذج التسجيل
│   ├── Review.php      # نموذج التقييم
│   └── Cart.php        # نموذج السلة
├── api/
│   ├── auth.php        # نقاط نهاية المصادقة
│   ├── courses.php     # نقاط نهاية الدورات
│   ├── lessons.php     # نقاط نهاية الدروس
│   ├── categories.php  # نقاط نهاية التصنيفات
│   ├── cart.php        # نقاط نهاية السلة
│   └── reviews.php     # نقاط نهاية التقييمات
└── uploads/            # مجلد الملفات المرفوعة
```

## 🚀 التثبيت

1. **إعداد قاعدة البيانات:**
   ```bash
   mysql -u root -p < ../sql/database.sql
   ```

2. **تكوين الاتصال:**
   - عدّل ملف `config/database.php` وأدخل بيانات الاتصال الصحيحة

3. **إعداد الخادم:**
   - تأكد من تثبيت PHP 7.4+ مع PDO extension
   - أنشئ مجلد `uploads` وامنحه صلاحيات الكتابة

## 📡 API Endpoints

### المصادقة (auth.php)
| Method | Endpoint | الوصف |
|--------|----------|-------|
| POST | `?action=register` | إنشاء حساب جديد |
| POST | `?action=login` | تسجيل الدخول |
| POST | `?action=logout` | تسجيل الخروج |
| GET | `?action=profile` | عرض الملف الشخصي |
| PUT | `?action=update-profile` | تحديث الملف الشخصي |
| PUT | `?action=change-password` | تغيير كلمة المرور |

### الدورات (courses.php)
| Method | Endpoint | الوصف |
|--------|----------|-------|
| GET | `?action=list` | قائمة الدورات |
| GET | `?action=featured` | الدورات المميزة |
| GET | `?action=get&id=X` | تفاصيل دورة |
| POST | `?action=create` | إنشاء دورة |
| PUT | `?action=update&id=X` | تحديث دورة |
| DELETE | `?action=delete&id=X` | حذف دورة |
| POST | `?action=enroll&id=X` | التسجيل في دورة |
| GET | `?action=my-courses` | دوراتي |

### الدروس (lessons.php)
| Method | Endpoint | الوصف |
|--------|----------|-------|
| GET | `?action=list&course_id=X` | دروس الدورة |
| GET | `?action=get&id=X` | تفاصيل درس |
| POST | `?action=create` | إضافة درس |
| PUT | `?action=update&id=X` | تحديث درس |
| DELETE | `?action=delete&id=X` | حذف درس |
| POST | `?action=complete` | إكمال درس |

### التصنيفات (categories.php)
| Method | Endpoint | الوصف |
|--------|----------|-------|
| GET | `?action=list` | قائمة التصنيفات |
| POST | `?action=create` | إنشاء تصنيف |
| PUT | `?action=update&id=X` | تحديث تصنيف |
| DELETE | `?action=delete&id=X` | حذف تصنيف |

### السلة (cart.php)
| Method | Endpoint | الوصف |
|--------|----------|-------|
| GET | `?action=list` | محتوى السلة |
| POST | `?action=add` | إضافة للسلة |
| DELETE | `?action=remove&course_id=X` | إزالة من السلة |
| DELETE | `?action=clear` | تفريغ السلة |
| POST | `?action=checkout` | إتمام الشراء |

### التقييمات (reviews.php)
| Method | Endpoint | الوصف |
|--------|----------|-------|
| GET | `?action=list&course_id=X` | تقييمات الدورة |
| POST | `?action=create` | إضافة تقييم |
| PUT | `?action=update&id=X` | تحديث تقييم |
| DELETE | `?action=delete&id=X` | حذف تقييم |
| GET | `?action=stats&course_id=X` | إحصائيات التقييم |

## 🔐 المصادقة

استخدم JWT Token في الـ Header:
```
Authorization: Bearer YOUR_TOKEN_HERE
```

## ✅ التحقق من المدخلات

جميع المدخلات يتم التحقق منها باستخدام فئة `Validator`:
- البريد الإلكتروني
- كلمة المرور (6 أحرف على الأقل)
- الحقول المطلوبة
- الحدود القصوى والدنيا
- أنواع الملفات

## 🔒 الأمان

- تشفير كلمات المرور باستخدام bcrypt
- حماية من SQL Injection باستخدام PDO prepared statements
- تنظيف المدخلات من XSS
- مصادقة JWT
- CORS headers
